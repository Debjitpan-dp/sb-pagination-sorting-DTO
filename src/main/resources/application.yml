spring:
  application:
  name: PaginationSortingDtoConversionExample
  datasource:
    url: jdbc:postgresql://localhost:5432/test-pagination
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create